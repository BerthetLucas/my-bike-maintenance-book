export interface BicycleRequest {
  name: string;
  isMarked: boolean;
}

export interface Bicycle {
  id: string;
  name: string;
  isMarked: boolean;
}

export interface NewBicycle {
  name: string;
  isMarked: boolean;
}
